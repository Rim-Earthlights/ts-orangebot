export const CONFIG = {
    // common settings
    COMMON: {
        DEV: true,
        HOSTNAME: '0.0.0.0',
        PORT: 4000,
        HOST_URL: 'http://exsample.com',
        USER: 'webUser'
    },
    // discord app settings
    DISCORD: {
        // your discord bot app id
        APP_ID: '*******************',
        // privillage role id (rule readed member role)
        MEMBER_ROLE_ID: '*******************',
        // admin user id (privillage reset gacha etc...)
        ADMIN_USER_ID: ['*******************'],
        // register command
        COMMAND_GUILD_ID: ['*******************'],
        // discord bot token
        TOKEN: '******.*********.****-*****-*****'
    },
    GACHA: {
        PICKRATE: 1.00
    },
    // OpenWeatherMap API KEY
    // OpenWeatherMap; see: https://openweathermap.org/
    FORECAST: {
        // if disabled, still empty.
        KEY: ''
    },
    // YOUTUBE API KEY
    // Google Cloud Platform; see: https://console.cloud.google.com/apis/api/youtube.googleapis.com/metrics
    YOUTUBE: {
        // if disabled, still empty.
        KEY: ''
    },
    // OPENAI API KEY / TOKEN
    // KEY:
    // OpenAI > https://platform.openai.com/
    // TOKEN:
    // GET > https://chat.openai.com/api/auth/session and find accessToken
    OPENAI: {
        // if disabled, still empty.
        KEY: '',
        // if enable gpt-4 in your api, turn true.
        IS_ENABLE_GPT4: false,
        // if disabled, still empty.
        ACCESSTOKEN:
            ''
    },
    NICONICO: {
        ENABLE: true,
        MAIL: '',
        PASSWORD: ''
    },
    DB: {
        HOSTNAME: '127.0.0.1',
        USERNAME: 'user',
        DATABASE: 'database',
        PASSWORD: 'password',
        PORT: 3306,
        FLUSH: false
    }
};
